return {
    -- Server-authoritative prices (used when charging at the pump - milestone 2).
    -- Kept here so the client cannot tamper with the real amount billed.
    literPrice = 1.7,
    kwPrice    = 0.3,
}
